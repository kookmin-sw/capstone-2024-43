{
  "name": "bookapi",
  "version": "0.0.1",
  "description": "moonhyang_bookAPI",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "zip -r t.zip . && aws lambda update-function-code --function-name getBook --zip-file fileb://./t.zip && rm t.zip"
  },
  "author": "Hyeonseok Yoo",
  "dependencies": {
    "xml2js": "^0.6.2"
  }
}
